웹노벨 변수 문법
만약 스프레드 시트가 새로고침해도 업데이트 반영이 되지 않는다면, 탭을 닫았다가 다시 열어주세요
그럼에도 여전히 반영이 안된다면 ctrl+shift+R을 눌러 브라우저 캐시를 삭제하고 다시 시도해주세요! (단, 이거 하면 세이브 날아감)

* 띄어쓰기 지키셔야 됩니다..!!
* 여깄는 변수이름, 값들은 한글이어도 됩니다
* 사용하실 때
- 대사A 출력 끝난 뒤 대사B로 넘어가기 전에 먹이고 싶은 경우 dialogues의 대사A 부분 행의 F열(command)에 써주세요
- 선택지를 눌렀을 때 즉후에 if를 하고 싶으신 경우 choices의 S열(command)에 작성해주세요
* 씬이나 변수 이름을 if, then, else, as, go, random, showif 등의 예약어로 짓지 말아주세요!! 

혹시나 나중에 개발 들어가면서 문법 몇개 바뀔수도 있긴 하겠지만 일단 대체로 안바꾸고 구현할 수 있도록 노력하려함니다


dialogues 시트에 있는 F열 모습 -

choices 시트에 있는 S열 모습

ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
## 변수 관련

<변수 저장하기>
set 변수이름 as 값
* 값은 정수 또는 문자열로 입력하시되, 문자열의 경우 양끝에 "을 붙여주세요 (예시: set drink as "coffee" 또는 set drink as 0)

<변수에 숫자 더하기>
add 값 to 변수이름
* 변수에 숫자를 저장하셨을 경우 추가로 숫자를 더할 수 있습니다 값을 음수로 쓰면 뺄셈이 됩니다 (쓸일이 있을지는 모르겠음)

<변수 삭제>
delete 변수이름
* 변수를 아예 삭제해버립니다 (쓸일이 있을지는 모르겠음)

ㅡㅡㅡㅡㅡㅡ
## 선택지 또는 변수 값 비교해서 분기 결정하는 방법

*변수이름 대신 씬이름, 호감도명 넣어도 동작하게 만들예정 이므로 같은 문법으로 씬이름 넣으셔도 될것같습니다

<변수 값 비교 참인지>
if 변수이름 == 값 then 분기할씬이름
* 참일경우 분기하고 거짓인 경우 그냥 아무일도 없었던척 다음 대사를 출력합니다 (다음 대사가 없다면 scenes 시트에서 현재씬에 저장해둔 다음씬으로 가질 것임)
* 예시: if meat == 1 then scene1


<변수 값 비교 거짓인지>
if 변수이름 != 값 then 분기할씬이름
* 거짓일경우 분기하고 참인 경우 그냥 아무일도 없었던척 다음 대사를 출력합니다 (다음 대사가 없다면 아마 scenes 시트에서 현재씬에 저장해둔 다음씬으로 가질 것임)
* 예시: if meat != 1 then scene2


<변수 값 비교 참, 거짓에 따른 구분>
if 변수이름 == 값A then 씬A else 씬B
* 변수이름이 가진 값과 값A을 비교해서 참인경우 씬A로 가고 거짓일경우 씬B로 갑니다 (만약 변수이름이 저장된게 없는 경우 scenes 시트에서 현재씬에 저장해둔 다음씬으로 가질 것임)
* 예시: if meat == 1 then scene1 else scene2


<변수 값 비교 참, 거짓에 따른 구분>
if 변수이름 != 값A then 씬B else 씬A
* 변수이름이 가진 값과 값A을 비교해서 거짓인경우 씬B로 가고 참일경우 씬A로 갑니다 (만약 변수이름이 저장된게 없는 경우 scenes 시트에서 현재씬에 저장해둔 다음씬으로 가질 것임)
* 예시: if meat != 1 then scene2 else scene1

<변수 값 and 적용>
if 변수1 == 값1 and 변수2 == 값2 then 씬A
* 두 조건 모두 참일경우 동작합니다
* 예시: if liszt >= 50 and scene1 == 1 then scene_9

<변수 값 크기 비교>
if 변수이름 < 값 then 씬A
* 변수이름 < 값일시 씬A. 거짓인 경우 그냥 아무일도 없었던척 다음 대사를 출력합니다 (다음 대사가 없다면 scenes 시트에서 현재씬에 저장해둔 다음씬으로 가질 것임)
* 예시: if mendelssohn < 50 then scene1
* < 말고 >으로 하면 변수이름>값 으로 비교하고 동작함 (이하동문). 이상, 이하는 각각 >=, <=
* 얘도 뒤에 'else 씬2' 같은거 붙여도 동작합니다 (이하동문)


<동일한지 한번에 비교하기>
ifs 변수이름 == 값1,값2,값3 then 씬1,씬2,씬3
* 변수이름의 값이 값1일경우 씬1로, 값2일경우 씬2로 ... 이런식으로 갑니다. 분리는 콤마(,)로 하고 개수제한은 없습니다 (만약 나열한 모든 경우에 해당 안하거나, 변수이름이 저장된게 없는 경우 scenes 시트에 저장해둔 다음씬으로 가짐)
* 예시: if scene1_choice is 1,2,3,4,"epic" then scene11,scene12,scene13,scene14,scene_epic


<변수이름의 값 크기 한번에 비교해서 넘기기>
ifs 변수이름 >= 값1,값2,값3 then 씬1,씬2,씬3
* 변수이름의 값을 값1,값2,값3순서대로 크기 비교해서 참인게 걸리면 거기로 이동합니다(<=, >, < 써도 됨.)
* (만약 모든 경우에 해당 안하거나, 변수이름이 저장된게 없는 경우 scenes 시트에 저장해둔 다음씬으로 가짐)
* 예시: if mendelssohn > 50,30,10,0 then scene_love,scene_friend,scene_normal,scene_bad


ㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡㅡ
## 랜덤 돌리기
random 시작범위~끝범위 go 범위1~범위2:씬1,범위3~범위4:씬2,범위5~범위6:씬3

* 시작부터 끝범위에 대한 주사위를 돌립니다.. 값은 정수로 입력하고 꼭 모든 범위를 포함되게 해주세요 (만약 누락 있을 시 현재씬에 기록된 다음씬으로 넘기거나, 현재씬의 다음 대사 나옴) 씬은 콤마로 구분합니다. go 뒤에 오는 범위에 물결이 없으면 숫자 범위 1개로 간주합니다
* 예시: random 1~10 go 1~3:scene_30,4:scene_10,5~10:scene_60
(30%확률로 scene_30, 10%확률로 scene_10, 60%확률로 scene_60이 나오는 이벤트)
* 예시: random 1~2 go 1:scene_A, 2:scene_B
(50% 확률로 씬A 또는 B로 감)
.





ㅡㅡㅡㅡㅡㅡㅡㅡ

## 선택지 숨기기 기능

* choices 시트의 R열(show_if)에 작성해주시면 됩니다! 위쪽거랑 문법이 다릅니다. R열이 비어있으면 해당 선택지는 언제나 보여줍니다
* 변수이름 대신 씬이름, 호감도 모두 쓸 수 있습니다

<변수 값 비교>
showif 변수이름 == 값
showif 변수이름 < 값
showif 변수이름 <= 값
showif 변수이름 >= 값
showif 변수이름 > 값
showif 변수이름 != 값
* 변수이름 (연산자) 값이 참일 때 이 선택지가 보이게 합니다. 거짓인 경우 숨깁니다
* 이게 전부임













.

